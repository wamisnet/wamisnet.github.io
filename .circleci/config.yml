
version: 2
jobs:
  build:
    docker:
      - image: cibuilds/hugo:latest
    steps:
      - checkout
      - run:
          name: chmod permissions
          command: chmod +x ./Deploy.sh
      - run:
          name: Upload to Deploy
          command: ./Deploy.sh
